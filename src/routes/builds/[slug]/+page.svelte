<script lang="ts">
	// This page is never rendered — the server load redirects to the planner.
	// Kept as a fallback in case the redirect doesn't fire.
</script>

<section class="mx-auto max-w-4xl px-6 py-16">
	<p class="text-[var(--hud-muted)]">Redirecting to build planner...</p>
</section>
